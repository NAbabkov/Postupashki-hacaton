# Происхождение сохранённых данных

Все `live_*` каталоги получены реальными публичными HTTPS-запросами 12.09.2026,
без аккаунта, cookies, приватных API или скачивания вложений. Точное время,
URL и SHA-256 каждой страницы находятся в `raw/*.meta.json` и `coverage.json`.

| Каталог | Назначение |
| --- | --- |
| `live_postypashki_old_20260912` | Основной взрослый канал, 5 страниц, 86 увиденных/54 выбранных поста |
| `live_postupashki_20260912` | Связанное школьное направление, 3 страницы, 42 увиденных/27 выбранных постов |
| `replay_postypashki_old_20260912` | Повторный разбор исходного adult snapshot, без новой загрузки |
| `replay_postupashki_20260912` | Повторный разбор исходного school snapshot, без новой загрузки |
| `sandbox_dns_error_20260912` | Реальная DNS-ошибка sandbox, ноль полученных HTTP-ответов и постов |
| `inspection` | Первичный HTML основной публичной страницы для исследования наблюдаемого `before` |

Оба JSONL в каждом исходном снимке и его replay совпали. Журнал проверки
находится в `validation_report.txt`. Нижняя граница 04.08.2026 достигнута в обоих
live snapshot; навигация к более старым страницам не исчерпана. Пропуски
неизвестны, полный архив не заявлен. `postupashki` не включается в основной
взрослый маркетинговый корпус автоматически.

В `inspection/provenance.json` время первого файла указано по filesystem mtime:
в первой одноразовой команде точное время начала запроса отдельно не записывалось.
Это явно отличается от `collected_at` полноценных запусков CLI.
